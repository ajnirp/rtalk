<% publish_to room_path(@room) do %>
	$("#chat").append("<%= j render partial: 'rooms/message', locals: {message: @message} %>");
<% end %>
$("#chat").scrollTop($("#chat")[0].scrollHeight); //scroll down once a new message is added
$("#new_message")[0].reset(); //reset the input form